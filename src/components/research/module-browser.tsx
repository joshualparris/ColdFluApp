"use client";import {useMemo,useState} from "react";type Item={slug:string;title:string;category:string;origin:"imported"|"canonical";status:string};
export function ModuleBrowser({items}:{items:Item[]}){const[q,setQ]=useState(""),[origin,setOrigin]=useState("all"),[category,setCategory]=useState("all");const shown=useMemo(()=>items.filter(x=>(origin==="all"||x.origin===origin)&&(category==="all"||x.category===category)&&`${x.title} ${x.slug}`.toLowerCase().includes(q.toLowerCase())),[items,q,origin,category]);return <section><h2>Module browser</h2><div className="filters"><label>Search<input value={q} onChange={e=>setQ(e.target.value)}/></label><label>Origin<select value={origin} onChange={e=>setOrigin(e.target.value)}><option value="all">All</option><option value="imported">Imported</option><option value="canonical">Canonical</option></select></label><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}><option value="all">All</option>{[...new Set(items.map(x=>x.category))].map(x=><option key={x}>{x}</option>)}</select></label></div><p>{shown.length} records</p><div className="module-list">{shown.map(x=><a className="module-link" href={`/research-preview/${x.origin}/${x.slug}`} key={`${x.origin}-${x.slug}`}><strong>{x.title}</strong><span>{x.origin} · {x.category} · {x.status}</span></a>)}</div></section>}

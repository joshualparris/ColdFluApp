"use client";import {useMemo,useState} from "react";export type BrowserItem={slug:string;title:string;category:string;origin:"imported"|"canonical";status:string;evidenceReview:string;clinicalReview:string;editorialReview:string};
export function ModuleBrowser({items}:{items:BrowserItem[]}){const[q,setQ]=useState(""),[origin,setOrigin]=useState("all"),[category,setCategory]=useState("all"),[status,setStatus]=useState("all"),[view,setView]=useState("all");const shown=useMemo(()=>items.filter(x=>(origin==="all"||x.origin===origin)&&(category==="all"||x.category===category)&&(status==="all"||x.status===status)&&(view==="all"||view===x.category||view==="human"&&[x.evidenceReview,x.clinicalReview,x.editorialReview].some(y=>y!=="complete")||view==="evidence"&&x.status==="evidence-review"||view==="intake"&&x.status==="intake-audit")&&`${x.title} ${x.slug}`.toLowerCase().includes(q.toLowerCase())),[items,q,origin,category,status,view]);return <section><h2>Choose your path</h2><div className="filters"><label>View<select value={view} onChange={e=>setView(e.target.value)}><option value="all">Continue current work</option><option value="symptom">By symptom</option><option value="intervention">By intervention</option><option value="population">By special population</option><option value="prevention">By prevention</option><option value="triage">By triage</option><option value="human">Blocked by human review</option><option value="intake">Ready for intake audit</option><option value="evidence">Ready for evidence review</option></select></label><label>Search<input value={q} onChange={e=>setQ(e.target.value)}/></label><label>Origin<select value={origin} onChange={e=>setOrigin(e.target.value)}><option value="all">All</option><option value="imported">Imported</option><option value="canonical">Canonical</option></select></label><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}><option value="all">All</option>{[...new Set(items.map(x=>x.category))].sort().map(x=><option key={x}>{x}</option>)}</select></label><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="all">All</option>{[...new Set(items.map(x=>x.status))].sort().map(x=><option key={x}>{x}</option>)}</select></label></div><p>{shown.length} records</p><div className="module-list">{shown.map(x=><a className="module-link" href={`/research-preview/${x.origin}/${x.slug}`} key={`${x.origin}-${x.slug}`}><strong>{x.title}</strong><span>{x.origin} · {x.category} · {x.status}</span><small>Evidence: {x.evidenceReview}; clinical: {x.clinicalReview}; editorial: {x.editorialReview}</small></a>)}</div></section>}

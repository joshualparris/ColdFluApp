"use client";import {useMemo,useState} from "react";type Media={id:string;title:string;format:string;organisation:string;url:string;topics:string[];jurisdiction:string;verifiedAt:string;notes:string};export function MediaBrowser({items}:{items:Media[]}){const[q,setQ]=useState(""),[format,setFormat]=useState("all");const shown=useMemo(()=>items.filter(x=>(format==="all"||x.format===format)&&`${x.title} ${x.organisation} ${x.topics.join(" ")}`.toLowerCase().includes(q.toLowerCase())),[items,q,format]);return <><div className="filters"><label>Search<input value={q} onChange={e=>setQ(e.target.value)}/></label><label>Format<select value={format} onChange={e=>setFormat(e.target.value)}><option value="all">All</option>{[...new Set(items.map(x=>x.format))].map(x=><option key={x}>{x}</option>)}</select></label></div><p>{shown.length} curated records</p>{shown.map(x=><article className="card" key={x.id}><h2><a href={x.url}>{x.title}</a></h2><p>{x.format} · {x.organisation} · {x.jurisdiction}</p><p>{x.notes}</p><small>Link checked {x.verifiedAt}</small></article>)}</>}

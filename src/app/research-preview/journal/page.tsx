import {getResearchAssignments} from "@/lib/private-preview/assignments";import {ExpeditionJournal} from "@/components/research/expedition-journal";export default async function Page(){const modules=await getResearchAssignments();return <><p className="eyebrow">Expedition journal</p><h1>Field notes and discoveries</h1><p>Keep personal observations beside the programme without mixing them into canonical evidence.</p><ExpeditionJournal modules={modules.map(x=>({slug:x.slug,title:x.module}))}/></>}

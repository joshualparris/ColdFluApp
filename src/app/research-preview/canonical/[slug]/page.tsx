import {notFound} from "next/navigation";import {getCanonicalModules,getCanonicalSources} from "@/lib/content/canonical";import {CanonicalModuleView} from "@/components/modules/canonical-module";
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const[m,s]=await Promise.all([getCanonicalModules(),getCanonicalSources()]);const record=m.find(x=>x.slug===slug);if(!record)notFound();return <><aside className="danger"><strong>PRIVATE CANONICAL DRAFT</strong><p>Status: {record.status}. This view does not approve the record.</p></aside><CanonicalModuleView module={record} sources={s}/></>}

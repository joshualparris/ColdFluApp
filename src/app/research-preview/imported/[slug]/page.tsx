import {notFound} from "next/navigation";import {getImportedDrafts,getImportedMarkdown,getImportedSources} from "@/lib/private-preview/imported";
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const[drafts,sources,markdown]=await Promise.all([getImportedDrafts(),getImportedSources(),getImportedMarkdown(slug)]);const draft=drafts.find(x=>x.slug===slug);if(!draft)notFound();const used=sources.filter(x=>draft.sourceIds.includes(x.id));return <article><aside className="danger"><strong>PRIVATE IMPORTED DISCOVERY DRAFT</strong><p>This AI-assisted material has not completed claim-level source verification or independent review. It must not be treated as medical advice or copied into public content.</p></aside><p className="eyebrow">Imported discovery draft · {draft.section}</p><h1>{draft.title}</h1><p className="lede">{draft.summary}</p><p><strong>Draft generation date:</strong> {draft.reviewDate}</p><h2>Prototype evidence snapshot</h2><p>{draft.evidenceSnapshot}</p>{draft.interventions.map(x=><article className="card" key={x.name}><h3>{x.name}</h3><p><span className="badge">Prototype label: {x.evidence}</span></p><p>{x.finding}</p><p><strong>Practical suggestion:</strong> {x.practical}</p><p><strong>Cautions:</strong> {x.cautions}</p></article>)}<h2>Red flags and unsettled questions</h2><ul>{[...draft.redFlags,...draft.unsettled].map(x=><li key={x}>{x}</li>)}</ul><h2>Candidate sources</h2><ul>{used.map(x=><li key={x.id}><a href={x.url}>{x.title}</a><br/><strong>Candidate source — exact metadata and claim applicability not independently verified.</strong> {x.organisation}</li>)}</ul>{markdown&&<section><h2>Imported Markdown summary</h2><pre className="research-text">{markdown}</pre></section>}</article>}
